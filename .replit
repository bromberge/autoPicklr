run = "pip install -q -r requirements.txt && uvicorn main:app --host 0.0.0.0 --port 8080 --log-level debug"
modules = ["python-3.11"]

[[ports]]
localPort = 5000
externalPort = 80

[nix]
channel = "stable-25_05"
